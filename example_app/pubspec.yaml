name: otel_provider_example
publish_to: none
description: Runnable Flutter demo of `otel_provider` exporting OTLP telemetry.

environment:
  sdk: ^3.0.0
  flutter: ">=3.10.0"

dependencies:
  dartastic_opentelemetry: ^1.1.0-beta.12
  flutter:
    sdk: flutter
  otel_provider:
    path: ..
  provider: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
